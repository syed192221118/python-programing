
odd_sum = 0
even_sum = 0


n = int(input("Enter the number of elements: "))


for i in range(n):
    element = int(input(f"Enter element {i+1}: "))
    if element % 2 == 0:
        even_sum += element ** 2
    else:
        odd_sum += element ** 2


print("Output:")
print([odd_sum, even_sum])
